//! OtterZip debug CLI.
//! Not shipped — developer/tester convenience only.

fn main() {
    println!("OtterZip CLI v{}", otterzip_core::version());
    println!("Debug tool. For end users, use OtterZip.exe (WinUI3).");
}

//! SpanZIP debug CLI.
//! Not shipped — developer/tester convenience only.

fn main() {
    println!("SpanZIP CLI v{}", spanzip_core::version());
    println!("Debug tool. For end users, use SpanZIP.exe (WinUI3).");
}

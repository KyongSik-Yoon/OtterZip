[package]
name = "spanzip-cli"
description = "SpanZIP — debug CLI (developer tool)"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true
rust-version.workspace = true

[[bin]]
name = "spanzip-cli"
path = "src/main.rs"

[dependencies]
spanzip-core = { path = "../spanzip-core" }

# OtterZip — Third-Party Notices

OtterZip's archive engine is a Rust core that links the open-source components
listed below (target `x86_64-pc-windows-msvc`). Each is distributed under its
own license; the full license texts follow.

> Auto-generated by `cargo about` via `tools/gen-notices.ps1`. Do not edit by
> hand — regenerate before each release.

## Summary

{{#each overview}}
- **{{name}}** — {{count}} component(s)
{{/each}}

---

{{#each licenses}}
## {{name}}

{{#if used_by}}
Bundled by:
{{#each used_by}}
- `{{crate.name}}` {{crate.version}}
{{/each}}
{{/if}}

```
{{{text}}}
```

{{/each}}
